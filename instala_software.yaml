---
    - name: Instala software via choco
      hosts: all
      gather_facts: no
      vars:
        app: "{{ps_app}}"
      tasks:
        - name: "Instalando"
          win_shell:  choco install "{{ps_app}}" -y
          register: powershell
        - debug: msg={{powershell}}    
